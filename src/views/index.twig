{% extends "./_layout.twig" %}

{% block page %}
<link href="
https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/css/splide.min.css
" rel="stylesheet">
<style>
    .splide {
        padding: 0 0 2rem;
    }
    .splide__arrow {
        background-color: #4A5568;
        width: 40px;
        height: 40px;
    }
    .splide__arrow svg {
        width: 16px;
        fill: #F2F2F2;
    }
    .splide__pagination__page {
        background-color: #F2F2F2;
    }
    .splide__pagination__page.is-active {
        background-color: #4A5568;
    }
    @media (max-width: 767px) {
        .splide__slide img {
            height: auto;
        }
    }
    @media (min-width: 768px) {
        .splide__slide img {
            object-fit: cover;
            height: 420px;
            width: 100%;
        }
    }
</style>
    <main class="content">
        <section class="splide lazy-load full-width" aria-label="Splide Basic HTML Example">
            <div class="splide__track">
                <ul class="splide__list">
                    {% for index, slide in slidesMyPhotoPages %}
                        <li class="splide__slide">
                            <a href="{{ slide.link }}">
                                <img
                                {% if index == 0 %} fetchpriority="high" {% else %} fetchpriority="low" {% endif %}
                                data-splide-lazy-srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                data-splide-lazy="{{ slide.mobile.jpg }}" width="100%" height="420"
                                alt="{{ slide.alt }}" title="{{ slide.title }}"> 
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </section>
        {# <section class="splide lazy-load full-width">
            <div class="splide__track">
                <ul class="splide__list">
                    {% for index, slide in slidesFineArtPrint %}
                        <li class="splide__slide">
                            <img
                            {% if index == 0 %}
                                fetchpriority="high"
                                srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                src="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% else %}
                                fetchpriority="low"
                                data-splide-lazy-srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                data-splide-lazy="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% endif %}
                            alt="{{ slide.alt }}" title="{{ slide.title }}"> 
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </section> #}
        {# <section class="splide lazy-load full-width">
            <div class="splide__track">
                <ul class="splide__list">
                    {% for index, slide in slidesFineArtBox %}
                        <li class="splide__slide">
                            <img
                            {% if index == 0 %}
                                fetchpriority="high"
                                srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                src="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% else %}
                                fetchpriority="low"
                                data-splide-lazy-srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                data-splide-lazy="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% endif %}
                            alt="{{ slide.alt }}" title="{{ slide.title }}"> 
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </section> #}
        {# <section class="splide lazy-load full-width">
            <div class="splide__track">
                <ul class="splide__list">
                    {% for index, slide in slidesFineArtBox %}
                        <li class="splide__slide">
                            <img
                            {% if index == 0 %}
                                fetchpriority="high"
                                srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                src="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% else %}
                                fetchpriority="low"
                                data-splide-lazy-srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                data-splide-lazy="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% endif %}
                            alt="{{ slide.alt }}" title="{{ slide.title }}"> 
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </section> #}
        {# <section class="splide lazy-load full-width">
            <div class="splide__track">
                <ul class="splide__list">
                    {% for index, slide in slidesCB %}
                        <li class="splide__slide">
                            {% if slide.link is not empty %}
                            <a href="{{ slide.link }}">
                            {% endif %}
                            <img
                            {% if index == 0 %}
                                fetchpriority="high"
                                srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                src="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% else %}
                                fetchpriority="low"
                                data-splide-lazy-srcset="{{ slide.mobile.webp }} 768w, {{ slide.desktop.webp }}"
                                data-splide-lazy="{{ slide.mobile.jpg }}" width="100%" height="420"
                            {% endif %}
                            alt="{{ slide.alt }}" title="{{ slide.title }}">
                            {% if slide.link is not empty %}
                            </a>
                            {% endif %}
                            
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </section> #}
    </main>
    <script src="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/js/splide.min.js"></script>
    <script>
        new Splide('.splide', {
            type: 'loop',
            arrows: false,
            autoplay: true,
            interval: 4000,
            pauseOnHover: false,
            lazyLoad: 'nearby',
            arrows: true
        }).mount();
    </script>
<!-- /.content -->
{% endblock %}